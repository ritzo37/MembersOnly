<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add message :</title>
  </head>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #f0f4f8;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    form {
      background-color: #ffffff;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      width: 90%;
      max-width: 500px;
    }

    label {
      display: block;
      font-size: 1rem;
      color: #333333;
      margin-bottom: 0.5rem;
    }

    textarea {
      width: 100%;
      height: 150px;
      padding: 0.75rem;
      font-size: 1rem;
      border: 1px solid #cccccc;
      border-radius: 8px;
      resize: vertical;
      margin-top: 0.5rem;
      margin-bottom: 1rem;
    }

    button {
      background-color: #4caf50;
      color: white;
      padding: 0.6rem 1.2rem;
      font-size: 1rem;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.2s ease-in-out;
    }

    button:hover {
      background-color: #45a049;
    }
    ul {
      list-style-type: none;
      padding: 1rem;
      background-color: #ffdddd;
      border: 1px solid #ff5c5c;
      border-radius: 8px;
      margin-bottom: 1rem;
      color: #a70000;
      max-width: 400px;
      margin-left: auto;
      margin-right: auto;
      box-shadow: 0 0 10px rgba(255, 0, 0, 0.1);
    }

    ul li {
      margin: 0.5rem 0;
      font-weight: 500;
    }
  </style>
  <body>
    <%- include("./error.ejs") %>
    <form action="/add-message/<%=username%>" method="post">
      <label for="text">
        Enter Your message ! Don't worry only secret members of the club can
        view your message and no one else !
        <textarea name="message"> </textarea>
        <button>Submit</button>
      </label>
    </form>
  </body>
</html>
